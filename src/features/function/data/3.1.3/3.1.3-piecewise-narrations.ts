// ============================================================
// 3.1.3 分段函数 - 知识解读文案
// 供 SpeakButton 朗读，用口语化表达，避免公式符号
// ============================================================

export const piecewiseNarrations = {
  intro:
    '欢迎来到分段函数。分段函数是高中数学里一个很重要的概念，它就是"一个函数，根据自变量的范围不同，用不同的表达式来计算"。听起来复杂，其实你生活中早就见过了：比如出租车计价，3公里以内按起步价，超过3公里再按每公里单独计费。这就是分段计算的思路。这节课我们从概念出发，学会求值、求定义域值域，再到分段函数的单调性。',

  concept:
    '什么是分段函数。分段函数的本质，是同一个函数在不同的自变量范围内，用不同的解析式来表示。注意两点：第一，它仍然是一个函数，不是多个函数；第二，每个自变量只属于一段，不会同时落在两段里。书写时用大括号，左边写各段的表达式，右边写各段的定义域条件。求值时，先看自变量落在哪一段，再用那一段的表达式代入计算。',

  domain:
    '分段函数的定义域和值域。分段函数的定义域，就是各段定义域的并集，把所有段的自变量范围合并起来。值域也一样，是各段值域的并集。求值域时，分别对每一段求出它能取到的函数值范围，最后合并。注意各段之间的端点，要看是否包含，用实心圆点还是空心圆点来区分。',

  monotonicity:
    '分段函数的单调性。判断一个分段函数在整个定义域上单调递增，需要同时满足两个条件：第一，每一段在自己的区间内是增函数；第二，在段与段的衔接处，左段的最大值必须小于或等于右段的最小值，也就是不能出现"右段比左段结尾低"的情况。这两个条件缺一不可。反过来，如果只满足每段各自递增，但衔接处"跳低"了，整体就不是单调递增的。',
} as const;
